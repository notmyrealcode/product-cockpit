{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${env:HOME}/shepherd-test"
      ],
      "outFiles": [
        "${workspaceFolder}/out/extension.js"
      ],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
